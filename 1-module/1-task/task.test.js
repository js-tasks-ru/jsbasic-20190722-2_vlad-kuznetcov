describe('1-module-1-task', () => {
  it('при возведении 2 в 0ю степень результат 1', () => {
    expect(pow(2, 0)).toEqual(1);
  });

  it('при возведении 3 в 4ю степень равен 81', () => {
    expect(pow(3, 4)).toEqual(81);
  });
});
